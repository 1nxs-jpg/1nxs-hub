# Only hub scripts should be tracked — this is a script repo, not MCP source
# (local MCP infra should NOT be committed)

# Build artifacts
dist/*

# node modules
node_modules/*

# pnpm store
.pnpm-store/*

# Cursor
.cursor/*
.cursor/*

# macOS
.DS_Store

# MCP infrastructure — keep local only
src/
docs/
scripts/
.agents/
jailbreak/
# connector.luau and demonology_hub.lua are hub scripts — keep in repo
bun.lock
package-lock.json
package.json
pnpm-lock.yaml
tsconfig.json
GEMINI.md
README.md
LICENSE
